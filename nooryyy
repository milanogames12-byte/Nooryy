<!DOCTYPE html>
<html lang="ar">
<head>
<meta charset="UTF-8">
<title>👀</title>

<style>
body {
  margin: 0;
  background: black;
  color: white;
  font-family: Arial, sans-serif;
  height: 100vh;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

#step1, #step2 {
  text-align: center;
}

input {
  padding: 10px;
  font-size: 18px;
  border-radius: 8px;
  border: none;
  outline: none;
}

button {
  margin-top: 15px;
  padding: 10px 25px;
  font-size: 18px;
  border-radius: 8px;
  border: none;
  cursor: pointer;
}

#step2 {
  display: none;
  font-size: 48px;
  font-weight: bold;
}

.explosion {
  position: absolute;
  width: 10px;
  height: 10px;
  background: orange;
  border-radius: 50%;
  animation: boom 1s linear forwards;
}

@keyframes boom {
  0% { transform: scale(1); opacity: 1; }
  100% { transform: scale(15); opacity: 0; }
}
</style>
</head>

<body>

<div id="step1">
  <h2>اكتبي اسمك 👀</h2>
  <input id="name" placeholder="اسمك هنا">
  <br>
  <button onclick="next()">التالي</button>
</div>

<div id="step2">
  مش مهتممممممممممممم
</div>

<script>
function next() {
  document.getElementById("step1").style.display = "none";
  document.getElementById("step2").style.display = "block";

  setTimeout(startExplosions, 2000);
}

function startExplosions() {
  setInterval(() => {
    const e = document.createElement("div");
    e.className = "explosion";
    e.style.left = Math.random() * window.innerWidth + "px";
    e.style.top = Math.random() * window.innerHeight + "px";
    document.body.appendChild(e);

    setTimeout(() => e.remove(), 1000);
  }, 100);
}
</script>

</body>
</html>
